version: '2.4'

services:
  # Servicio de la API
  api:
    build: .
    container_name: senati-blog-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
